name: Data Processing Pipeline

on:
  push:
    tags:
      - 'v*' # Run on tags like v0.1, v1.0.0
  workflow_dispatch: # Allows manual triggering

permissions:
  contents: write # Required for creating releases

jobs:
  process-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Setup ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install . # install the current package and its dependencies

      - name: Clean data
        run: invoke clean-data

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: data/processed/*
          fail_on_unmatched_files: true # Fail if no files are found to upload

      # - name: Configure AWS Credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      #     aws-region: auto
      #     endpoint-url: https://2dbc8ec4fc56f55a49b6accffcea925a.r2.cloudflarestorage.com

      # - name: Upload to R2
      #   run: |
      #     # Upload CSV files with appropriate content type
      #     aws s3 sync data/processed/ s3://${{ secrets.R2_BUCKET_NAME }}/data/ \
      #       --endpoint-url https://2dbc8ec4fc56f55a49b6accffcea925a.r2.cloudflarestorage.com \
      #       --exclude "*" \
      #       --include "*.csv" \
      #       --content-type "text/csv" \
      #       --cache-control "public, max-age=86400"
          
      #     # Upload Parquet files with appropriate content type
      #     aws s3 sync data/processed/ s3://${{ secrets.R2_BUCKET_NAME }}/data/ \
      #       --endpoint-url https://2dbc8ec4fc56f55a49b6accffcea925a.r2.cloudflarestorage.com \
      #       --exclude "*" \
      #       --include "*.parquet" \
      #       --content-type "application/vnd.apache.parquet" \
      #       --cache-control "public, max-age=86400"
